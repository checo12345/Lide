SELECT coverage_area._id, coverage_area.altitude, coverage_area.latitude, coverage_area.longitude, address.latitude, address.longitude FROM coverage_area LEFT JOIN address ON coverage_area._id = address._coverage_area_id AND address.orders = ( SELECT MAX( orders )  FROM address) WHERE coverage_area._id = 4785074604081152;
SELECT coverage_area._id, coverage_area.altitude, coverage_area.latitude, coverage_area.longitude, address.latitude, address.longitude FROM coverage_area LEFT JOIN address ON coverage_area._id = address._coverage_area_id AND address.orders = ( SELECT MAX( orders )  FROM address) WHERE (address.orders=?)